package main

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestDay2Example(t *testing.T) {
	const input = `
	`
	const output = `
	`

	assert.Equal(t, Day2(input), output)
}

func TestDay2(t *testing.T) {
	const input = ``
	const output = ``
	assert.Equal(t, Day2(input), output)
}
