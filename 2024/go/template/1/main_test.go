package main

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestDay1Example(t *testing.T) {
	const input = `
	`
	const output = `
	`

	assert.Equal(t, Day1(input), output)
}

func TestDay1(t *testing.T) {
	const input = ``
	const output = ``
	assert.Equal(t, Day1(input), output)
}
